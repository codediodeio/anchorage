<div class="panel panel-default">
  <div class="panel-body">
    <h4><%= link_to exp.user.username, profile_path(exp.user.username), class: "text-primary" %>'s Experience</h4>
    <%= sanitize(exp.body) %>
  </div>
</div>
