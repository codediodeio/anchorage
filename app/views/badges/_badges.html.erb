<% if badge.custom_fields[:difficulty] == :gold %>
<li><%= link_to badge_path(id: badge.id, name: badge.name), class: "gold-badge" do %>
  <i class="fa fa-certificate"></i> <%= badge.name %></li>
  <% end %>
  <% end %>

<% if badge.custom_fields[:difficulty] == :silver %>
<li><%= link_to badge_path(id: badge.id, name: badge.name), class: "silver-badge" do %>
  <i class="fa fa-certificate"></i> <%= badge.name %></li>
  <% end %>
  <% end %>

<% if badge.custom_fields[:difficulty] == :bronze %>
<li><%= link_to badge_path(id: badge.id, name: badge.name), class: "bronze-badge" do %>
  <i class="fa fa-certificate"></i> <%= badge.name %></li>
  <% end %>
  <% end %>
