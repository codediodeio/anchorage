  <% if user_signed_in? %>
  <%= link_to new_image_path(location_id: @location.id), remote: true, class: "btn btn-pink photo-btn-left" do %>
  <i class="fa fa-camera-retro"></i> Share Photo
  <% end %>
  <% else %>
  <%= link_to signup_path, remote: true, class: "btn btn-pink photo-btn-left" do %>
    <i class="fa fa-camera-retro"></i> Share Photos
  <% end %>
  <% end %>
